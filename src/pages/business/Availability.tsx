import { useTitle } from '../../hooks/useTitle'

const Availability = () => {
  useTitle('Manage Availability')

  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Manage Availability</h1>
    </div>
  )
}

export default Availability
