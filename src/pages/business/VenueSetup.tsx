import { useTitle } from '../../hooks/useTitle'

const VenueSetup = () => {
  useTitle('Venue Setup')

  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Venue Setup</h1>
    </div>
  )
}

export default VenueSetup
