import { useTitle } from '../../hooks/useTitle'

const VenueApprovals = () => {
  useTitle('Venue Approvals')

  return (
    <div className="p-8">
      <h1 className="text-2xl font-semibold">Venue Approvals</h1>
    </div>
  )
}

export default VenueApprovals
